1) grade_checker.py 

score=int(input("Enter your score : \n"))

if score >=90:
    print("Your Grade: A")
elif score >=80:
    print("Your Grade: B")
elif score >=70:
    print("Your Grade: C")
elif score >=60:
    print("Your Grade: D")
else:
    print("Your Grade: F")
 
2) student_grade.py 

# Provide choice to user 

response = input(f" Choose the option : a (Add a new student and grade): b (Update an existing student’s grade): c (Print all student grades)")

if response in ('a'):
    student_name = input('Enter the Student name: ')
    student_grade = input('Enter grade: ')
    student_result[student_name] = student_grade
    print(f"Added new student {student_name} with grade: {student_grade}")

elif response in ('b'):
    student_name = input(f"Enter the student name to be changed: ")
    student_grade = input(f"Corrected grade for {student_name} :")
    student_result[student_name] = student_grade
    print(f"Updated the student {student_name} with the new grade: {student_grade}")
    
elif response in ('c'):
    print(student_result)
    
else:
    print("invalid input")
    
3) write_to_file.py
# Create text file

c_file = open('myfile.txt', 'w')

#Write into the file 

c_file.write(""" This is a new file and I wanted to add this 
              This file is written by write_to_file.py""")

# Close the file 

c_file.close()

myfile.txt
 This is a new file and I wanted to add this 
              This file is written by write_to_file.py
	      
4) read_me.py 

# Read from the file 

read_me = open('myfile.txt', 'r+')

file_data = read_me.read()

print(file_data)

read_me.close()
